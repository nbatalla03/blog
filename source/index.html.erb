---
pageable: true
per_page: 10
---
<div class="container">
  <div class="row clearfix">
    <div class="column profile">
      <h3>Nelsol Batalla</h3>
    </div>

    <div class="column latest-post clearfix">
      <span class="marker">Latest Post</span>
        <ul>
          <% blog.articles.select {|a| a.data.category != "projects"}[0..10].each do |article| %>
            <li>
                <h2><a href="<%= article.url%>"><%= article.title %></a></h2>
                <%= article.summary %>
            </li>
          <% end %>
        </ul>
    </div>
  </div>
</div>
